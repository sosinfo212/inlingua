import type {Metadata} from "next";import Link from "next/link";import {Header,Footer,Cta} from "../components";import {articles} from "./articles";import "./blog.css";
export const metadata:Metadata={title:"Language access insights",description:"Practical articles about interpretation, translation, multilingual events, and language access planning.",alternates:{canonical:"/blog"}};
export default function Blog(){return <><Header/><main id="main"><section className="page-hero short"><div className="shell"><p className="eyebrow">INSIGHTS & GUIDES</p><h1>Better questions lead to clearer communication.</h1><p>Practical guidance for planning language services and building more accessible multilingual experiences.</p></div></section><section className="section shell"><div className="section-head"><p>LATEST ARTICLES</p><h2>Useful before your next request.</h2></div><div className="article-grid">{articles.map((a,i)=><article className="article-card" key={a.slug}><div className={`article-art art-${i+1}`} aria-hidden="true"><span>0{i+1}</span></div><div className="article-copy"><p>{a.category} · {a.readTime}</p><h2><Link href={`/blog/${a.slug}`}>{a.title}</Link></h2><p>{a.description}</p><Link className="article-link" href={`/blog/${a.slug}`}>Read the guide →</Link></div></article>)}</div></section><Cta/></main><Footer/></>}
