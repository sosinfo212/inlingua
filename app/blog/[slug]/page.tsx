import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {Header,Footer,Cta} from "../../components";import {articles,getArticle} from "../articles";import "../blog.css";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=getArticle(slug);if(!a)return {};return {title:a.title,description:a.description,alternates:{canonical:`/blog/${a.slug}`},openGraph:{type:"article",title:a.title,description:a.description},twitter:{card:"summary",title:a.title,description:a.description}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=getArticle(slug);if(!a)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:a.title,description:a.description,datePublished:"2026-08-01",dateModified:"2026-08-22",author:{"@type":"Organization",name:"inlingua Boston"},publisher:{"@type":"Organization",name:"inlingua Boston"}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><Header/><main id="main"><article><header className="article-hero"><div className="shell article-head"><Link href="/blog">← All insights</Link><p>{a.category} · {a.readTime}</p><h1>{a.title}</h1><p className="article-dek">{a.intro}</p><div className="article-byline"><span>inlingua Boston</span><span>{a.date}</span></div></div></header><div className="article-body shell"><aside><p>IN THIS GUIDE</p>{a.sections.map((s,i)=><a href={`#section-${i+1}`} key={s.heading}>{s.heading}</a>)}</aside><div className="article-content">{a.sections.map((s,i)=><section id={`section-${i+1}`} key={s.heading}><span>0{i+1}</span><h2>{s.heading}</h2>{s.body.map(p=><p key={p}>{p}</p>)}</section>)}<div className="article-next"><p>RELATED NEXT STEPS</p>{a.related.map(r=><Link href={r.href} key={r.href}>{r.label} <span>→</span></Link>)}</div></div></div></article><Cta/></main><Footer/></>}
